---
phase: 01-core-infrastructure
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - src/core/context.lua
  - src/core/registry.lua
  - tests/core/context_spec.lua
  - tests/core/registry_spec.lua
autonomous: true
requirements:
  - CORE-01
  - CORE-02
  - CORE-07
  - CORE-09

must_haves:
  truths:
    - "Context.new() with no transport option sets ctx.transport to a NullTransport instance"
    - "Context.new({ transport = true }) creates a real Transport with default mock channels and wires auto-bridge to bus"
    - "Context.new({ transport = instance }) uses the provided Transport and wires auto-bridge"
    - "Auto-bridge: bus:emit() for a networkable event also queues it on transport"
    - "Auto-bridge: bus:emit() for a non-networkable event does NOT queue on transport"
    - "Context passes config to Bus and Services for error_mode resolution"
    - "Registry resolves its own error_mode from config"
    - "Registry in strict mode propagates plugin init errors"
    - "Registry in tolerant mode logs plugin init errors and continues"
    - "Registry supports optional side enforcement on plugin manifest entries"
  artifacts:
    - path: "src/core/context.lua"
      provides: "Context factory with transport field, auto-bridge, config threading"
      exports: ["Context.new"]
    - path: "src/core/registry.lua"
      provides: "Plugin registry with error_mode and optional side enforcement"
      exports: ["Registry.new"]
    - path: "tests/core/context_spec.lua"
      provides: "Tests for transport wiring, auto-bridge, config threading"
    - path: "tests/core/registry_spec.lua"
      provides: "Tests for error_mode and side enforcement"
  key_links:
    - from: "src/core/context.lua"
      to: "src/core/transport.lua"
      via: "Transport.Null.new() or Transport.new() based on opts.transport"
      pattern: "Transport%.Null%.new"
    - from: "src/core/context.lua"
      to: "src/core/bus.lua"
      via: "Override bus:emit() to install auto-bridge for transport"
      pattern: "original_emit"
    - from: "src/core/registry.lua"
      to: "config.error_mode"
      via: "resolve_error_mode at construction or boot time"
      pattern: "error_mode"
---

<objective>
Wire Context assembly (transport field, auto-bridge, config threading) and add error_mode + side enforcement to Registry.

Purpose: Context is the assembly point where all core modules come together. After this plan, `Context.new()` produces a fully-wired ctx with transport always present (null or real), bus auto-bridged to transport for networkable events, and config threaded to modules. Registry gains error_mode support and optional side enforcement for dual-world setups.

Output: Updated context.lua and registry.lua with full wiring, updated specs for both.
</objective>

<execution_context>
@/home/botboy0/.claude/get-shit-done/workflows/execute-plan.md
@/home/botboy0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-infrastructure/01-CONTEXT.md
@.planning/phases/01-core-infrastructure/01-RESEARCH.md
@.planning/phases/01-core-infrastructure/01-01-SUMMARY.md
@.planning/phases/01-core-infrastructure/01-02-SUMMARY.md

<interfaces>
<!-- After Plan 01 — Worlds API: -->
function Worlds.create()                -- single-world (default): spawn() works, spawn_server/client error
function Worlds.create({ dual = true }) -- dual-world: spawn_server/client work, spawn() errors

<!-- After Plan 02 — Bus API: -->
function Bus.new(opts)  -- opts = { log = fn, error_mode = "strict"|"tolerant" }
-- backward compat: Bus.new(fn) treats fn as log, tolerant mode

<!-- After Plan 02 — Transport API: -->
function Transport.new(opts)    -- real transport with channels
Transport.Null.new()            -- null stub, all no-ops
function Transport:mark_networkable(event)
function Transport:is_networkable(event) -> bool
function Transport:queue(event, data)
function Transport:flush()
function Transport:receive() -> msg|nil
function Transport:receive_all() -> msgs[]

<!-- Current Context API (src/core/context.lua): -->
function Context.new(opts)  -- opts: { worlds, bus, config }
-- returns ctx = { worlds, bus, config, services }

<!-- Current Registry API (src/core/registry.lua): -->
function Registry.new()
function Registry:register(name, module, opts)  -- opts.deps = {...}
function Registry:boot(ctx)
function Registry:shutdown(ctx)

<!-- Error mode resolution pattern (from RESEARCH.md): -->
local function resolve_error_mode(config, module_name, fallback)
    local modes = config and config.error_modes
    if modes and modes[module_name] ~= nil then return modes[module_name] end
    if config and config.error_mode ~= nil then return config.error_mode end
    return fallback
end
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire Context with transport field, auto-bridge, and config threading</name>
  <files>src/core/context.lua, tests/core/context_spec.lua</files>
  <behavior>
    - Context.new({ worlds = w, bus = b }) with no transport → ctx.transport is NullTransport instance
    - Context.new({ transport = false }) → ctx.transport is NullTransport instance
    - Context.new({ transport = true }) → ctx.transport is a real Transport (needs mock channels for test)
    - Context.new({ transport = Transport.new({...}) }) → ctx.transport is the provided instance
    - When transport is real (not null): bus:emit(event, data) also calls transport:queue(event, data) for networkable events
    - When transport is null: bus:emit() works normally, no transport queue call
    - Auto-bridge does NOT break re-entrancy guard (transport:queue during bus flush is safe because bridge is on emit, not flush)
    - ctx.config is passed through and accessible
    - Bus receives error_mode from config (resolved as config.error_modes.bus or config.error_mode, fallback "tolerant")
    - Services error_mode resolved from config (config.error_modes.services or config.error_mode, fallback "strict")
  </behavior>
  <action>
Modify `src/core/context.lua`:

1. Add requires at top:
   ```lua
   local Transport = require("src.core.transport")
   ```

2. Add a local `resolve_error_mode` helper function:
   ```lua
   local function resolve_error_mode(config, module_name, fallback)
       local modes = config and config.error_modes
       if modes and modes[module_name] ~= nil then return modes[module_name] end
       if config and config.error_mode ~= nil then return config.error_mode end
       return fallback
   end
   ```

3. Update `Context.new(opts)`:
   - Resolve bus error_mode: `resolve_error_mode(opts.config, "bus", "tolerant")`
   - Create bus with error_mode if opts.bus is not provided, or note that bus is pre-created and may need error_mode set. Since bus is passed in pre-created, Context should accept an already-created bus. The error_mode threading for bus will be handled by the caller (main.lua in Plan 04). Context just stores what it receives.
   - Actually, to keep it simple and honor the assembly pattern: Context.new receives pre-built bus and worlds. Config threading for bus happens at construction time in main.lua. Context just assembles.
   - **Transport wiring:**
     - `opts.transport == nil or false` → `ctx.transport = Transport.Null.new()`
     - `opts.transport == true` → `ctx.transport = Transport.new({ outbound_channel = ..., inbound_channel = ... })` — but this requires channels. For `transport = true`, create Transport with opts from config or error if no channels provided. For Phase 1, `transport = true` requires `opts.transport_channels = { outbound = ch, inbound = ch }` or we defer the "auto-create channels" to main.lua. Simplest: `transport = true` is handled by main.lua which creates Transport with real channels, then passes the instance. Context sees only `nil/false` (→ null) or `<instance>` (→ use it). The `transport = true` shorthand is resolved in main.lua before Context.new. Update: per CONTEXT.md, `transport = true` means "framework creates Transport with sensible default channels." This belongs in Context.new or a factory. For testability, let Context.new handle `transport = true` by requiring opts.transport_channels when transport is true. If not provided, error in strict mode or use no-op in tolerant.
     - **Simplification:** Context.new handles three cases:
       1. `opts.transport` is falsy → NullTransport
       2. `opts.transport` is a table with `mark_networkable` method (duck-type check) → use as-is
       3. `opts.transport == true` → error with message "transport = true requires opts.transport_channels = { outbound = channel, inbound = channel }" (main.lua will provide these)
   - **Auto-bridge installation** (when transport is real, not null):
     ```lua
     if transport ~= null_instance then  -- or check a flag
         local original_emit = bus.emit
         bus.emit = function(self_bus, event, data)
             original_emit(self_bus, event, data)
             if transport:is_networkable(event) then
                 transport:queue(event, data)
             end
         end
     end
     ```
     Use a type check: if `getmetatable(transport) ~= NullTransport_mt` or simpler: add a `transport._is_null` flag on NullTransport. Or just always install the bridge — NullTransport:is_networkable always returns false, so queue is never called. This is cleaner.
     **Decision:** Always install the bridge. NullTransport.is_networkable returns false, so the queue call never happens. No special-casing needed.

4. Add `transport` to the ctx table returned by Context.new.

5. For Services error_mode: modify Services.new to accept an error_mode param. In tolerant mode, Services:get returns nil instead of erroring (actually per CONTEXT.md, services default is strict — keep the error). Services:register in tolerant mode logs instead of erroring on duplicate. Thread this through.

Update `tests/core/context_spec.lua`:
- Add tests for `ctx.transport`:
  - No transport option → ctx.transport exists, is_networkable returns false (NullTransport)
  - transport = false → same as above
  - transport = Transport.new({mock channels}) → ctx.transport is the real instance
  - transport = true without channels → errors
- Add tests for auto-bridge:
  - Create real transport with mock channels, mark event as networkable
  - bus:emit(networkable_event) → transport queue has the event after flush
  - bus:emit(non_networkable_event) → transport queue is empty
- Add tests for config passthrough

Run `stylua src/core/context.lua tests/core/context_spec.lua` after editing.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Trynda/Desktop/Dev/Lua/Love2D/Love2DFramework && busted tests/core/context_spec.lua</automated>
  </verify>
  <done>
    - ctx.transport is always present (NullTransport or real Transport)
    - Auto-bridge forwards networkable events from bus to transport
    - Non-networkable events are not forwarded
    - Config is threaded through to ctx
    - All context_spec tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add error_mode and optional side enforcement to Registry</name>
  <files>src/core/registry.lua, tests/core/registry_spec.lua</files>
  <behavior>
    - Registry.new() with no config defaults to strict error_mode (broken boot = crash)
    - Registry.new({ config = { error_mode = "tolerant" } }) stores tolerant mode
    - Registry.new({ config = { error_modes = { registry = "tolerant" } } }) resolves per-module override
    - Strict boot: plugin:init() error propagates, boot aborts
    - Tolerant boot: plugin:init() error is logged, boot continues with remaining plugins
    - Tolerant boot: failed plugin is excluded from _boot_order (not shut down later)
    - Side enforcement (when plugin entry has side = "server" or "client"):
      - In dual-world mode: server plugin cannot depend on client plugin, and vice versa
      - In single-world mode: side declarations are ignored (no enforcement)
      - validate_deps checks side constraints when worlds are dual-mode
    - Shutdown behavior unchanged (reverse order, skip plugins without shutdown method)
  </behavior>
  <action>
Modify `src/core/registry.lua`:

1. Add `resolve_error_mode` helper (same pattern as context.lua — or extract to a shared util; for now, duplicate is fine since it's 5 lines):
   ```lua
   local function resolve_error_mode(config, module_name, fallback)
       local modes = config and config.error_modes
       if modes and modes[module_name] ~= nil then return modes[module_name] end
       if config and config.error_mode ~= nil then return config.error_mode end
       return fallback
   end
   ```

2. Change `Registry.new(opts)` to accept optional opts:
   ```lua
   function Registry.new(opts)
       opts = opts or {}
       local config = opts.config or {}
       return setmetatable({
           _plugins = {},
           _boot_order = {},
           _booted = false,
           _error_mode = resolve_error_mode(config, "registry", "strict"),
           _log = opts.log or print,
       }, Registry)
   end
   ```

3. Update `Registry:register(name, plugin_module, opts)` to also accept `opts.side`:
   ```lua
   table.insert(self._plugins, {
       name = name,
       module = plugin_module,
       deps = opts.deps or {},
       side = opts.side,  -- nil, "server", or "client"
   })
   ```

4. Add side enforcement to `validate_deps` — accept an optional `dual_mode` flag:
   - If dual_mode is true and both the plugin and its dependency have `side` set, check they are compatible:
     - server plugin depending on client plugin → error
     - client plugin depending on server plugin → error
     - same side or either is nil → ok
   - If dual_mode is false or nil, skip side checks entirely.

5. Update `Registry:boot(ctx)`:
   - Determine dual_mode: check if `ctx.worlds` has both `.server` and `.client` fields (dual-world mode).
   - Pass dual_mode to `validate_deps`.
   - Wrap `entry.module:init(ctx)` in error handling based on error_mode:
     - Strict: let error propagate (current behavior)
     - Tolerant: `pcall(entry.module.init, entry.module, ctx)`, log on failure, skip failed plugin (don't add to _boot_order)
   - In tolerant mode, track which plugins succeeded so only those go into `_boot_order`.

Update `tests/core/registry_spec.lua`:
- Keep all existing tests (they use `Registry.new()` which defaults to strict — same as current).
- Add `describe("error_mode")` block:
  - Strict: plugin init error propagates from boot()
  - Tolerant: plugin init error is logged, boot continues, other plugins still init
  - Tolerant: failed plugin is not in shutdown order
- Add `describe("side enforcement")` block:
  - Dual-world: server plugin depending on client plugin → error
  - Dual-world: client depending on server → error
  - Dual-world: server depending on server → ok
  - Dual-world: plugin with no side → ok (no enforcement)
  - Single-world: side declarations ignored

Run `stylua src/core/registry.lua tests/core/registry_spec.lua` after editing.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Trynda/Desktop/Dev/Lua/Love2D/Love2DFramework && busted tests/core/registry_spec.lua</automated>
  </verify>
  <done>
    - Registry defaults to strict; tolerant catches init errors and continues
    - Side enforcement blocks cross-side deps in dual-world mode
    - Side declarations ignored in single-world mode
    - All existing registry_spec tests pass alongside new tests
  </done>
</task>

</tasks>

<verification>
1. `busted tests/core/context_spec.lua` — transport wiring, auto-bridge, config tests pass
2. `busted tests/core/registry_spec.lua` — error_mode and side enforcement tests pass
3. `busted` — full suite green (no regressions from Plan 01/02 changes)
4. `selene src/core/context.lua src/core/registry.lua` — no lint errors
5. `stylua --check src/core/context.lua src/core/registry.lua` — formatting correct
</verification>

<success_criteria>
- ctx.transport is always present after Context.new() — NullTransport when disabled, real Transport when provided
- Auto-bridge transparently forwards networkable bus events to transport
- Registry error_mode works: strict crashes on init failure, tolerant logs and continues
- Side enforcement blocks cross-side deps in dual-world, ignored in single-world
- Full test suite passes: `busted`
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-infrastructure/01-03-SUMMARY.md`
</output>

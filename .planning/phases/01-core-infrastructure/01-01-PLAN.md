---
phase: 01-core-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/components.lua
  - src/core/worlds.lua
  - tests/core/components_spec.lua
  - tests/core/worlds_spec.lua
autonomous: true
requirements:
  - CORE-05
  - CORE-06
  - CORE-08

must_haves:
  truths:
    - "Worlds.create() with no args returns a single-world handle with a spawn() method"
    - "Single-world spawn_server() and spawn_client() error immediately with a descriptive message"
    - "Worlds.create({ dual = true }) preserves current dual-world behavior with ServerTag/ClientTag"
    - "components.lua returns an empty table — no pre-defined fragments"
  artifacts:
    - path: "src/core/components.lua"
      provides: "Empty component registry table"
      contains: "return {}"
    - path: "src/core/worlds.lua"
      provides: "Single-world and dual-world modes"
      exports: ["Worlds.create", "Worlds.ServerTag", "Worlds.ClientTag"]
    - path: "tests/core/components_spec.lua"
      provides: "Asserts components.lua returns a table with no specific fields"
    - path: "tests/core/worlds_spec.lua"
      provides: "Tests for single-world spawn, dual-world spawn, mode errors"
  key_links:
    - from: "src/core/worlds.lua"
      to: "lib/evolved.lua"
      via: "evolved.spawn() wrapped by worlds:spawn()"
      pattern: "evolved%.spawn"
---

<objective>
Implement single-world mode for Worlds and clear components.lua to ship empty.

Purpose: Worlds is the ECS entry point for all plugins. Single-world mode (the default) simplifies onboarding for games that don't need client/server separation. Components.lua must ship empty so the framework is genre-agnostic — games define their own fragments.

Output: Updated worlds.lua with single/dual mode, empty components.lua, updated specs for both.
</objective>

<execution_context>
@/home/botboy0/.claude/get-shit-done/workflows/execute-plan.md
@/home/botboy0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-infrastructure/01-CONTEXT.md
@.planning/phases/01-core-infrastructure/01-RESEARCH.md

<interfaces>
<!-- Current Worlds API (src/core/worlds.lua): -->
Worlds.ServerTag, Worlds.ClientTag = evolved.id(2)
function Worlds.create()  -- returns { server = {tag}, client = {tag}, spawn_server(), spawn_client() }

<!-- Current components API (src/core/components.lua): -->
Components.Position, Components.Velocity, Components.Health = evolved.id(3)

<!-- evolved.lua key functions used: -->
evolved.id(n)         -- allocate n fragment IDs
evolved.spawn(comps)  -- spawn entity with component table
evolved.defer()       -- begin deferred operations
evolved.destroy(e)    -- mark entity for destruction
evolved.commit()      -- apply deferred operations
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add single-world mode to Worlds and update dual-world to opt-in</name>
  <files>src/core/worlds.lua, tests/core/worlds_spec.lua</files>
  <behavior>
    - Worlds.create() with no args returns single-world handle
    - Single-world: worlds:spawn({comps}) calls evolved.spawn() directly, no tags added
    - Single-world: worlds:spawn_server() errors with message containing "single-world"
    - Single-world: worlds:spawn_client() errors with message containing "single-world"
    - Single-world: worlds.server is nil, worlds.client is nil
    - Worlds.create({ dual = true }) returns dual-world handle (current behavior)
    - Dual-world: spawn_server() adds ServerTag, spawn_client() adds ClientTag
    - Dual-world: worlds.server.tag == Worlds.ServerTag, worlds.client.tag == Worlds.ClientTag
    - Dual-world: worlds:spawn() errors with message containing "dual-world" and hints to use spawn_server/spawn_client
    - Worlds.ServerTag and Worlds.ClientTag remain module-level constants (not reallocated per create())
  </behavior>
  <action>
Modify `src/core/worlds.lua`:

1. Change `Worlds.create(opts)` to accept an optional `opts` table.
2. Default behavior (no opts or `opts.dual` falsy) = single-world mode:
   - Return a table with `worlds:spawn(components)` that calls `evolved.spawn(components or {})` directly — no tags.
   - `worlds:spawn_server()` and `worlds:spawn_client()` call `error()` with a message like: `"spawn_server() is not available in single-world mode — use worlds:spawn() instead"`
   - `worlds.server` and `worlds.client` are nil (not set).
3. When `opts.dual == true` = dual-world mode (current behavior):
   - Preserve existing `spawn_server()` and `spawn_client()` with ServerTag/ClientTag.
   - Add `worlds:spawn()` that calls `error()` with: `"spawn() is ambiguous in dual-world mode — use spawn_server() or spawn_client()"`
   - Set `worlds.server = { tag = server_tag }` and `worlds.client = { tag = client_tag }` as currently done.
4. Keep `Worlds.ServerTag` and `Worlds.ClientTag` as module-level constants — unchanged.
5. Update the module docstring to reflect single-world default and dual opt-in.

Update `tests/core/worlds_spec.lua`:
- Keep all existing dual-world tests but wrap them in a `describe("dual-world mode")` block, changing `Worlds.create()` calls to `Worlds.create({ dual = true })`.
- Add a `describe("single-world mode")` block with tests for: spawn() works, spawn_server() errors, spawn_client() errors, server/client fields are nil.
- Add a `describe("dual-world mode")` test for: spawn() errors with hint message.
- Clean up evolved entities in after_each as existing tests do.

Run `stylua src/core/worlds.lua tests/core/worlds_spec.lua` after editing.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Trynda/Desktop/Dev/Lua/Love2D/Love2DFramework && busted tests/core/worlds_spec.lua</automated>
  </verify>
  <done>
    - Worlds.create() returns single-world handle; spawn() works, spawn_server/spawn_client error
    - Worlds.create({ dual = true }) returns dual-world handle with existing behavior; spawn() errors with hint
    - All worlds_spec tests pass (both old dual-world tests and new single-world tests)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Clear components.lua to ship empty and update specs</name>
  <files>src/core/components.lua, tests/core/components_spec.lua</files>
  <behavior>
    - require("src.core.components") returns a table
    - The returned table has no pre-defined fields (no Position, Velocity, Health)
    - Games add their own fragments: Components.X, Components.Y = evolved.id(2)
  </behavior>
  <action>
Replace the contents of `src/core/components.lua` with:

```lua
--- Shared component fragment definitions.
--- All fragment IDs are created here and used by both server and client worlds.
--- This is the single source of truth for component identity in the ECS.
---
--- This file ships EMPTY. Each game defines its own components:
---   Components.Position, Components.Velocity = evolved.id(2)
---
--- Do NOT define components in plugin files — only here.
--- The architecture validator (Phase 2) enforces this rule.

return {}
```

Note: The `evolved` require is removed since there are no `evolved.id()` calls. The file is purely a convention placeholder.

Update `tests/core/components_spec.lua`:
- Remove any assertions for specific fields (Position, Velocity, Health).
- Assert only: `require("src.core.components")` returns a table of type "table".
- Optionally assert the table is empty (`next(Components) == nil`) to confirm no framework-defined fragments leak.

Run `stylua src/core/components.lua tests/core/components_spec.lua` after editing.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Trynda/Desktop/Dev/Lua/Love2D/Love2DFramework && busted tests/core/components_spec.lua</automated>
  </verify>
  <done>
    - components.lua returns an empty table with no pre-defined fragments
    - components_spec.lua passes asserting table type and emptiness
    - No evolved.id() calls remain in components.lua
  </done>
</task>

</tasks>

<verification>
1. `busted tests/core/worlds_spec.lua` — all single-world and dual-world tests pass
2. `busted tests/core/components_spec.lua` — empty table assertions pass
3. `selene src/core/worlds.lua src/core/components.lua` — no lint errors
4. `stylua --check src/core/worlds.lua src/core/components.lua` — formatting correct
</verification>

<success_criteria>
- Worlds.create() defaults to single-world; Worlds.create({ dual = true }) opts into dual-world
- spawn(), spawn_server(), spawn_client() each error in the mode where they are not applicable
- components.lua ships with zero pre-defined fragment IDs
- All existing dual-world tests still pass (adapted to use { dual = true })
- Full spec suite green: `busted tests/core/worlds_spec.lua && busted tests/core/components_spec.lua`
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-infrastructure/01-01-SUMMARY.md`
</output>

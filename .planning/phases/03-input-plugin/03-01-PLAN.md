---
phase: 03-input-plugin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/baton.lua
  - src/core/registry.lua
  - tests/core/registry_spec.lua
  - main.lua
autonomous: true
requirements:
  - INPT-03

must_haves:
  truths:
    - "Registry:update_all(dt) calls plugin:update(dt) on every booted plugin that defines an update method"
    - "Plugins without an update method are silently skipped by update_all"
    - "main.lua calls _registry:update_all(dt) before bus:flush() in love.update"
    - "baton library is vendored at lib/baton.lua and require('lib.baton') succeeds"
  artifacts:
    - path: "lib/baton.lua"
      provides: "Vendored baton input library"
    - path: "src/core/registry.lua"
      provides: "Registry:update_all(dt) method"
      contains: "function Registry:update_all"
    - path: "main.lua"
      provides: "Plugin update loop before bus flush"
      contains: "update_all"
  key_links:
    - from: "main.lua"
      to: "src/core/registry.lua"
      via: "_registry:update_all(dt)"
      pattern: "_registry:update_all"
    - from: "src/core/registry.lua"
      to: "plugin:update(dt)"
      via: "iterates _boot_order calling update"
      pattern: "entry%.module:update"
---

<objective>
Add Registry:update_all(dt) lifecycle method and vendor the baton input library -- prerequisites for the input plugin.

Purpose: The current registry boots and shuts down plugins but has no per-frame update loop. The input plugin (plan 03-02) requires per-frame updates via baton. This plan adds the missing infrastructure and vendors the dependency.
Output: Registry with update_all(), main.lua calling it before bus:flush(), baton vendored in lib/
</objective>

<execution_context>
@/home/botboy0/.claude/get-shit-done/workflows/execute-plan.md
@/home/botboy0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-input-plugin/03-RESEARCH.md

@src/core/registry.lua
@main.lua
@tests/core/registry_spec.lua

<interfaces>
<!-- Registry API (from src/core/registry.lua): -->
Registry.new(opts) -> registry
Registry:register(name, plugin_module, opts)
Registry:boot(ctx)
Registry:shutdown(ctx)
-- _boot_order: array of { name, module, deps, side } for successfully booted plugins

<!-- main.lua locals: -->
local _registry   -- Registry instance
local _ctx         -- Context instance
-- love.update(dt) currently: transport:receive_all -> bus:flush -> transport:flush
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Registry:update_all(dt) + wire into main.lua</name>
  <files>src/core/registry.lua, tests/core/registry_spec.lua, main.lua</files>
  <behavior>
    - update_all(dt) iterates _boot_order and calls entry.module:update(dt) for each plugin that has an update method
    - Plugins without an update method are silently skipped (no error)
    - update_all before boot (empty _boot_order) is a no-op
    - update_all passes dt argument through correctly
    - In tolerant error_mode, a plugin update() error is logged and skipped (other plugins still update)
    - In strict error_mode (default), a plugin update() error propagates
  </behavior>
  <action>
1. In `src/core/registry.lua`, add `Registry:update_all(dt)` after the shutdown method:
   - Iterate `self._boot_order` (same order as boot)
   - For each entry: if `entry.module.update` exists (type check), call `entry.module:update(dt)`
   - In tolerant mode: wrap each call in pcall, log errors via `self._log`, continue
   - In strict mode: call directly (errors propagate)
   - Pattern matches shutdown's structure but calls `update` instead of `shutdown`

2. In `tests/core/registry_spec.lua`, add a new `describe("update_all")` block:
   - Test: calls update(dt) on plugins that have an update method
   - Test: skips plugins without update method (no error)
   - Test: passes dt argument through correctly
   - Test: tolerant mode logs and continues on update error
   - Test: strict mode propagates update error
   - Test: update_all before boot is a no-op (no error)
   - Use the existing test patterns (mock plugins with init/shutdown stubs)

3. In `main.lua`, add `_registry:update_all(dt)` in `love.update(dt)`:
   - Insert BEFORE the transport receive_all / bus:flush / transport:flush block
   - Guard with `if not _ctx then return end` (already exists)
   - The ordering becomes: update_all(dt) -> receive_all -> bus:flush -> transport:flush
   - This ensures plugin updates emit events that get flushed in the same frame
  </action>
  <verify>
    <automated>busted tests/core/registry_spec.lua</automated>
  </verify>
  <done>Registry:update_all(dt) exists, tested with 6+ test cases, main.lua calls it before bus:flush</done>
</task>

<task type="auto">
  <name>Task 2: Vendor baton library</name>
  <files>lib/baton.lua</files>
  <action>
1. Download baton.lua v1.0.2 from https://raw.githubusercontent.com/tesselode/baton/master/baton.lua
   - Use curl or wget to fetch the file
   - Save to `lib/baton.lua`

2. Verify the file is valid Lua:
   - Run `lua lib/baton.lua` or `luajit lib/baton.lua` -- should not error (module returns a table)
   - Confirm the file contains `baton.new` function

3. Note: lib/ is excluded from selene and stylua checks per CLAUDE.md, so no lint/format needed.
  </action>
  <verify>
    <automated>lua -e "local b = require('lib.baton'); assert(type(b.new) == 'function', 'baton.new missing'); print('baton vendored OK')"</automated>
  </verify>
  <done>lib/baton.lua exists, is valid Lua, exports baton.new function</done>
</task>

</tasks>

<verification>
1. `busted tests/core/registry_spec.lua` -- all tests pass including new update_all tests
2. `lua -e "local b = require('lib.baton'); assert(b.new)"` -- baton vendored correctly
3. `selene src/ main.lua conf.lua` -- no lint errors (baton in lib/ excluded)
4. `stylua --check src/ main.lua conf.lua` -- no format errors
5. `lua scripts/validate_architecture.lua` -- architecture validator passes
</verification>

<success_criteria>
- Registry:update_all(dt) method exists with error_mode support
- At least 6 test cases covering update_all behavior
- main.lua calls _registry:update_all(dt) before bus:flush() in love.update
- lib/baton.lua vendored and loadable
- Full CI pipeline passes (lint, format, tests, architecture validator)
</success_criteria>

<output>
After completion, create `.planning/phases/03-input-plugin/03-01-SUMMARY.md`
</output>

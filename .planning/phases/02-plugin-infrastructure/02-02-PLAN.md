---
phase: 02-plugin-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/validate_architecture.lua
  - tests/validate_architecture_spec.lua
autonomous: true
requirements:
  - PLUG-03
  - PLUG-04

must_haves:
  truths:
    - "Validator detects evolved.spawn() calls in plugin files as errors"
    - "Validator detects evolved.id() calls in plugin files as errors"
    - "Validator detects evolved.spawn/id aliases (local spawn = evolved.spawn) as errors"
    - "Validator warns on require('lib.evolved') in plugin files without failing CI"
    - "Validator exit code is 0 when only warnings exist, 1 when errors exist"
    - "Validator --verbose flag shows CLAUDE.md rule reference and surrounding code context"
    - "examples/ directory is NOT scanned for raw ECS calls"
  artifacts:
    - path: "scripts/validate_architecture.lua"
      provides: "detect_raw_ecs_calls function + verbose formatting + error/warning split in run()"
      contains: "detect_raw_ecs_calls"
    - path: "tests/validate_architecture_spec.lua"
      provides: "Tests for raw ECS call detection"
      contains: "detect_raw_ecs_calls"
  key_links:
    - from: "scripts/validate_architecture.lua detect_raw_ecs_calls"
      to: "Validator.run()"
      via: "Called for each plugin file, errors increment error_count, warnings increment warning_count"
      pattern: "detect_raw_ecs_calls"
    - from: "scripts/validate_architecture.lua run()"
      to: "script entry point"
      via: "Returns (error_count, warning_count); entry point exits on error_count > 0"
      pattern: "error_count.*warning_count"
---

<objective>
Add raw ECS call detection (evolved.spawn/evolved.id) to the architecture validator with verbose output and error/warning separation.

Purpose: PLUG-03 and PLUG-04 enforce that plugins use worlds:spawn_server()/spawn_client() instead of raw evolved API. The error/warning split ensures require("lib.evolved") warnings don't block CI while raw spawn/id calls do.
Output: Updated validate_architecture.lua with detect_raw_ecs_calls + verbose mode + dual return from run(). Updated tests.
</objective>

<execution_context>
@/home/botboy0/.claude/get-shit-done/workflows/execute-plan.md
@/home/botboy0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-plugin-infrastructure/02-CONTEXT.md
@.planning/phases/02-plugin-infrastructure/02-RESEARCH.md

@scripts/validate_architecture.lua
@tests/validate_architecture_spec.lua

<interfaces>
<!-- Current validator patterns the executor must follow. -->

From scripts/validate_architecture.lua:
```lua
-- Detection function signature pattern:
function Validator.detect_*(path, lines)
  -- path: string file path
  -- lines: string[] pre-read lines (optional, reads file if nil)
  -- returns: table[] violations (each has line_num, line, plus check-specific fields)
end

-- Helpers available:
local function read_lines(path) -- returns string[] or nil
local function find_lua_files(dir) -- returns string[] of .lua paths
local function is_excluded(path) -- checks EXCLUDED_DIRS
local function plugin_name_from_path(path) -- extracts plugin name from src/plugins/<name>/...

-- Current run() returns single number:
function Validator.run(opts) -- returns total (number)

-- Script entry uses arg[0] detection for CLI mode
```

From tests/validate_architecture_spec.lua:
```lua
-- Test pattern: pass lines directly to detect_* functions
local violations = Validator.detect_globals("/tmp/fixture.lua", lines)
-- Integration test calls Validator.run({ silent = true })
-- Currently asserts: assert.equals(0, total, ...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add detect_raw_ecs_calls detection function</name>
  <files>scripts/validate_architecture.lua</files>
  <behavior>
    - detect_raw_ecs_calls("src/plugins/foo/init.lua", lines_with_evolved_spawn) returns errors table with violation
    - detect_raw_ecs_calls("src/plugins/foo/init.lua", lines_with_evolved_id) returns errors table with violation
    - detect_raw_ecs_calls("src/plugins/foo/init.lua", lines_with_alias) returns errors with alias violation
    - detect_raw_ecs_calls("src/plugins/foo/init.lua", lines_with_require_evolved) returns empty errors, non-empty warnings
    - detect_raw_ecs_calls("src/core/bus.lua", any_lines) returns empty (non-plugin path)
    - detect_raw_ecs_calls("examples/canonical_plugin.lua", any_lines) returns empty (examples excluded)
    - Lines starting with -- (comments) are ignored
  </behavior>
  <action>
Add `Validator.detect_raw_ecs_calls(path, lines)` to validate_architecture.lua, placed after the existing detect_logic_outside_ecs function and before the report formatting section. The function:

1. **Path guard:** Return `{}, {}` immediately if path does not match `^src/plugins/`. This naturally excludes `examples/`, `src/core/`, `lib/`, etc. Add a comment: `-- examples/ and non-plugin paths excluded by this guard`.

2. **Read lines if nil** (standard pattern from other detect_* functions).

3. **Track aliases:** Scan each non-comment line for `local <name> = evolved.spawn` or `local <name> = evolved.id` patterns. When found, record the alias name and add an error with kind="alias" and message like `"evolved.spawn() alias -- use worlds:spawn_server() or worlds:spawn_client()"`. Per research pitfall 2, flag ONLY the alias assignment line as the error (do NOT track and flag subsequent alias calls -- this avoids false positives with variable shadowing).

4. **Direct calls:** Detect `evolved%.spawn%s*%(` and `evolved%.id%s*%(` patterns. Add error with kind="direct".

5. **Require warning:** Detect `["\']lib%.evolved["\']` pattern. Add warning (not error) with message `"require(\"lib.evolved\") in plugin -- consider using ctx.worlds for entity management"`.

6. **Return:** `return errors, warnings` (two tables).

Message format per CONTEXT.md spec:
- Errors: `"evolved.spawn() -- use worlds:spawn_server() or worlds:spawn_client()"`
- Warnings: `"require(\"lib.evolved\") in plugin -- consider using ctx.worlds for entity management"`
  </action>
  <verify>
    <automated>busted tests/validate_architecture_spec.lua</automated>
  </verify>
  <done>detect_raw_ecs_calls correctly identifies evolved.spawn, evolved.id, and aliases as errors; require("lib.evolved") as warnings; ignores comments and non-plugin paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire detection into run(), add verbose flag, update exit code logic, and add tests</name>
  <files>scripts/validate_architecture.lua, tests/validate_architecture_spec.lua</files>
  <behavior>
    - Validator.run() returns (error_count, warning_count) instead of single total
    - Validator.run() with verbose=true includes rule reference and context lines in output
    - Script entry point exits 0 on warnings only, exits 1 on errors
    - Existing integration smoke test still passes (updated for new return signature)
  </behavior>
  <action>
1. **Verbose formatting (validate_architecture.lua):** Add a local `format_verbose(file, line_num, lines, message, rule_ref)` helper before `print_section`. It returns a string with:
   - Line 1: `file:line_num: message`
   - Line 2: `  Rule: rule_ref` (if provided)
   - Lines 3-5: `  > N:  line_content` for line_num-1, line_num (with `  <--` marker), line_num+1

2. **Update print_section:** Add optional `verbose` and `lines_cache` parameters. When verbose is true, use `format_verbose` instead of the simple formatter. For new checks that return lines in their violations, pass `v.lines` or re-read file.

3. **Add a `print_warning_section` function** (or modify print_section to accept a severity label). Warnings should print with `[WARNING]` prefix to distinguish from errors.

4. **Update Validator.run():**
   - Accept `opts.verbose` (boolean, default false)
   - Parse `--verbose` from CLI args in the script entry point and pass to run()
   - Track `error_count` and `warning_count` separately (not a single `total`)
   - After existing checks 1-4 (which all contribute to `error_count`):
     - Add check 5: raw ECS calls. For each plugin file, call `detect_raw_ecs_calls(path, lines)`. Errors go to `error_count`, warnings to `warning_count`. Print errors via print_section with "Raw ECS Calls" title. Print warnings via warning section with "ECS Require Warnings" title.
   - Return `error_count, warning_count`

5. **Update script entry point:**
   - Parse `--verbose` flag from arg table
   - Call `Validator.run({ fix = fix, verbose = verbose })`
   - Capture `local errs, warns = Validator.run(...)`
   - Print warning count if > 0: `"(" .. warns .. " warning(s))"`
   - Exit 0 if `errs == 0`, exit 1 if `errs > 0`

6. **Update tests (validate_architecture_spec.lua):**
   - Update the existing `Validator.run` integration test to use `local errs, _ = Validator.run(...)` for the new dual return
   - Add `describe("Validator.detect_raw_ecs_calls", ...)` with test cases:
     - `it("flags evolved.spawn() direct call as error")`
     - `it("flags evolved.id() direct call as error")`
     - `it("flags evolved.spawn alias assignment as error")`
     - `it("warns on require('lib.evolved') without error")`
     - `it("ignores non-plugin paths")`
     - `it("ignores comment lines")`
     - `it("ignores examples/ paths")`
   - Each test passes `(path, lines)` directly to detect_raw_ecs_calls and asserts on the returned errors/warnings tables.

CLAUDE.md rule references for verbose mode:
- evolved.spawn/id: `'CLAUDE.md SS1 -- "All game logic MUST live in ECS systems"'`
- require("lib.evolved"): `'CLAUDE.md SS5 -- "No global mutable state outside the ECS world"'`
  </action>
  <verify>
    <automated>busted tests/validate_architecture_spec.lua && lua scripts/validate_architecture.lua</automated>
  </verify>
  <done>Validator.run() returns (error_count, warning_count). Verbose flag threads through to output. Exit code 0 on warnings-only. All existing + new tests pass. Architecture validation of the actual project still passes.</done>
</task>

</tasks>

<verification>
- `busted tests/validate_architecture_spec.lua` passes (all detection tests + updated smoke test)
- `lua scripts/validate_architecture.lua` exits 0 on the clean project
- `lua scripts/validate_architecture.lua --verbose` runs without error (verbose mode works)
- `busted` (full suite) passes with no regressions
</verification>

<success_criteria>
- evolved.spawn() in a plugin file is flagged as an error (CI fails)
- evolved.id() in a plugin file is flagged as an error (CI fails)
- Alias assignments (local spawn = evolved.spawn) are flagged as errors
- require("lib.evolved") in a plugin file produces a warning (CI does NOT fail)
- Verbose mode shows CLAUDE.md rule reference and 3 lines of context
- Validator.run() returns two values: error_count, warning_count
- Exit code logic uses error_count only (warnings don't block CI)
</success_criteria>

<output>
After completion, create `.planning/phases/02-plugin-infrastructure/02-02-SUMMARY.md`
</output>
